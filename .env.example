###########################################################################
# REQUIRED SETTINGS
###########################################################################

# Personal Access Token (PAT) used to register runners.
# Required scopes:
#   - "admin:org"       for organization runners
#   - "repo"            for repository runners
#   - "manage_runners:enterprise" for enterprise runners
#
# Example: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ACCESS_TOKEN=

# Choose EXACTLY ONE of the scopes below.
# Uncomment the one you’re actually using.

# For an organization‑level runner (recommended)
ORG=

# For a repository‑level runner
# REPO=your-org/your-repo

# For an enterprise‑level runner
# ENTERPRISE=your-enterprise


###########################################################################
# OPTIONAL SETTINGS (safe defaults provided)
###########################################################################

# Comma‑separated runner labels in addition to "self-hosted"
# e.g. LABELS=self-hosted,ubuntu-latest,amd64
LABELS=self-hosted,linux,x64

# Directory inside the container where jobs execute
# (usually fine to leave default)
RUNNER_WORKDIR=/runner/_work

# Host path for persistent runner registration/config data.
# Defaults to ./runner-data relative to the compose file.
# You can override it to an absolute path (e.g. /data/github-runners)
# when you want to use faster or larger storage.
#RUNNER_ROOT=/data/github-runners

###########################################################################
# ADVANCED SETTINGS
###########################################################################

# You normally don’t need to set this — each container infers
# its own name from the hostname when scaling with 
# `docker compose up -d --scale runner=N`
#RUNNER_NAME=my-runner-name

###########################################################################
# END OF CONFIG
###########################################################################